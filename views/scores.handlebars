<div class="container text-center">
  <div class="row align-items-center">
    <div class="col" id="col1">
      <img src="images/logo.png" alt="Gods and Beasts" width="500" height="600">
    </div>
    <div class="col" id="col2">
      <h1>Your top scores</h1>
    </div>
    {{!-- fetch method with get /api/users --}}
  </div>
</div>
<script>
  async function getHighScores() {
    var response = await fetch("/api/users/getuser", { //returns Promise object with all the content hidden inside it?
      method: "GET",
      headers: { "Content-Type": "application/json" },
    });
    if (response.ok) {
      response = response.json(); 
      console.log(response);
    }
    var column = document.querySelector("#col2")
    var scoretext = document.createElement('p');
    var winstext = document.createElement('p');
    var losstext = document.createElement('p');
    scoretext.textContent = response.highScore;
    winstext.textContent = response.wins;
    losstext.textContent = response.losses;
    column.append(scoretext, winstext, losstext);
  }
  getHighScores();
</script>

{{!--
<script src="./js/scores.js"></script> --}}